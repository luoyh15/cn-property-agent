# Do not commit secrets.
AMAP_API_KEY=

# Local path to a recorded Lianjia transaction snapshot. Required when the
# active city profile names the "lianjia" transaction provider; paths are
# machine-specific and must not be committed to a city profile.
CN_PROPERTY_LIANJIA_TRANSACTION_SNAPSHOT_PATH=
